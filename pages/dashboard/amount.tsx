import { Layout } from "@/components/dashboard";
import Amounts from "@/components/dashboard/pages/amounts";
import React from "react";

const Amount = () => {
  return (
    <Layout>
      <Amounts />
    </Layout>
  );
};

export default Amount;
