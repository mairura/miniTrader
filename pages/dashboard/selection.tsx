import { Layout } from '@/components/dashboard';
import { RiskSelection } from '@/components/dashboard';
import React from 'react';

const Dashboard = () => {
  return (
    <Layout>
      <RiskSelection />
    </Layout>
  );
};

export default Dashboard;
